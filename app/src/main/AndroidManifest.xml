<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.example.chowmedown" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_dog"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppStyle"
        android:usesCleartextTraffic="true" >
        <!-- android:theme="@style/Theme.Chowmedown"> -->
        <activity android:name=".MainActivity2" />
        <activity android:name=".Register" />
        <activity android:name=".LoginAct" />
        <activity android:name=".recipies" />
        <activity android:name=".newDogAdd" />
        <activity android:name=".DogActivity" /> <!-- android:theme="@android:style/Theme.Light.NoTitleBar"> -->
        <activity android:name=".profileAct" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>